{
  "framework": null,
  "buildCommand": "npm run build:shared --prefix .. && npm run build",
  "installCommand": "npm install --prefix ..",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}

{
  "framework": null,
  "buildCommand": "cd .. && npx tsc -p shared/tsconfig.json && npx tsc -p server/tsconfig.json",
  "installCommand": "npm install --prefix ..",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}

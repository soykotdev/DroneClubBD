# Vite only reads .env files from this (client/) directory — copy this to
# client/.env for local development. Only VITE_-prefixed variables are ever
# exposed to the browser bundle; never put MONGODB_URI or any server secret
# in a file in this directory.
VITE_API_BASE_URL=http://localhost:4000/api
